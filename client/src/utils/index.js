import initialState from './initialState';
import errorHandler from './errorHandler';
import fileEventAdapter from './fileEventAdapter';
import transformFormErrors from './transformFormErrors';

export default {
  initialState,
  errorHandler,
  transformFormErrors,
  fileEventAdapter
};
