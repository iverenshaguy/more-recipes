import reducer from './reducers';
import authOperations from './operations';
import authSelectors from './selectors';
import authTypes from './types';

export { authOperations, authSelectors, authTypes };

export default reducer;
